# Progress log

One short block per stage. Read this first when resuming — it's the source of truth for what's actually done, not `build-plan.md`'s static table.

---

## Stage 0 — Single-facility migration + mock layer + agent-kit install (2026-09-19)

**Gap list:** `facilityId` was threaded through session/DTO types, page props, and the header's facility switcher; `services/facility.service.ts` took an id per call; `usersService.create` sent `facilityId`; My profile showed a raw "Facility ID". Access control (`lib/access-control.ts`) was never keyed on facility identity, so no logic there needed to change.

**Done:**
- `facilityService.get()`/`update()` are now a singleton contract (`GET`/`PUT /facility`, no id), with a one-time fallback to the legacy `/facilities/{id}` (session's `facilityId`) on 404/405, dev-only warning logged.
- `facilityId` dropped from `FacilitySettingsWorkspace`, `UsersManagementWorkspace`, `usersService.create` payload, and My profile. Kept only as optional/`@deprecated` on `AuthUser`, the session payload, and DTOs (`UserListItem`, `CreateUserPayload`, `EncounterDto`, `AuditEventDto`).
- Header facility badge is a static label for every role, including super admin (SUP-01/SUP-02 retired — see `flows/actors/super-admin.md`).
- New `hooks/use-facility.ts` (TanStack Query) is the one place facility identity is read from; `AppHeader` uses it with a session-field fallback while loading.
- Installed the agent kit at the repo root (`docs/agents/`, `.claude/skills/nhims-*`, `.claude/agents/nhims-*`) — was nested under `frontend/nhims-frontend-agent-kit/`, moved up so the harness discovers the skills/subagents. Merged `AGENTS.md`/`CLAUDE.md`.
- Mock layer built: `services/mocks/mock-config.ts` (`isMockEnabled`/`isAnyMockEnabled`, reads `NEXT_PUBLIC_MOCK_AREAS`), `services/mocks/with-mock.ts` (`withMock`, `?mockError=` hook), dev-only "Sample data" pill wired into `AppHeader`.
- Docs updated: `07-access-and-navigation.md` (facility-services line, `/facility` endpoint), `super-admin.md` (SUP-01/02 retired), `build-plan.md` (Phase 6 super admin row, backend-gaps seed), `02-design-system.md` §3, new `backend-gaps.md`.

**Mocks added:** none used yet (nothing was missing in this stage — infrastructure only).

**Reviewer:** access-checker pass (via general-purpose stand-in, since `.claude/agents/*` weren't registered yet this session) — clean, no blocking/should-fix findings, no access regressions.

**Open issues:** none. Backend gap logged: `GET`/`PUT /facility` singleton endpoint doesn't exist yet; frontend runs on the legacy-fallback path.

**Commit:** `e89c723`

---

## Stage 1 — Authentication and account (2026-09-19)

**Gap list (from `flows/actors/shared-all-staff.md`):** login form had no intro sentence, always showed the TOTP field (jargon label "Authenticator code"), no locked/offline states, and the *login* password field was wrongly validated against new-password complexity rules (a real bug — would reject valid existing passwords). No forgot-password UI existed despite the service methods already being real. Change-password forms had no live rule checklist, duplicated schemas in two places, and generic error handling. Security page was a raw TOTP setup dump (secret + otpauth URI as text, no QR code, "TOTP" everywhere). Profile page showed a raw "User ID", used an ad-hoc `formatRole` instead of a central label map, and a bare spinner for loading. No `/login?reason=expired`/`?next=` handling in the 401 interceptor. No `app/not-found.tsx`. Header bell had nothing to show (no feed, mocked or otherwise) and no deep-linking. `MEDICAL_OFFICER` landed on `/nurse` instead of `/opd`. No Feedback entry point existed.

**Foundation built (just-in-time):**
- FND-01: `lib/status-labels.ts` (`ROLE_LABELS`/`roleLabel`), `lib/notify.ts`, `lib/api-errors.ts` (`getFriendlyError`, full 04§3.1 status table).
- FND-05: `components/layouts/page-card.tsx` (`PageCard`, `page-header.tsx` now aliases it — was dead code, zero call sites, so safe), `components/common/inline-notice.tsx`, `components/common/success-panel.tsx`, `components/common/no-access-notice.tsx`; `ConfirmDialog` now requires `confirmLabel` and defaults `cancelLabel` to "Go back".
- FND-08: removed the now-fully-dead `dashboard-*` CSS (classes + tokens) — confirmed zero usages first.
- FND-10: `DEFAULT_MODULE_BY_ROLE.MEDICAL_OFFICER` → `"opd"`.
- Also built (not a named FND but shared across slices): `schemas/password.schema.ts` (`PASSWORD_RULES`, `newPasswordSchema`) + `components/common/password-rule-checklist.tsx`, used by ALL-02/ALL-03/ALL-05.

**Slices done:**
- ALL-01 Sign in — intro sentence, TOTP field hidden until the server says it's needed (`totp-required`/`totp-invalid` result kinds) or the user opts in via "I use a code from my phone", locked/inactive/expired/offline states, "Forgot your password?" link, fixed the login-password-complexity-regex bug, removed the credential-in-URL leak path (dropped the form's `action` attribute).
- ALL-02 Choose a new password (forced) — live rule checklist, exact spec copy, success toast, dotted-canvas background (was plain grey).
- ALL-03 Forgot / reset password — new `ForgotPasswordForm` (`/login?step=forgot`) and `ResetPasswordForm` (`/reset-password?token=`), both against the already-real `authService.forgotPassword`/`resetPasswordWithToken`.
- ALL-04 My profile — simplified to spec (name, job via `roleLabel`, facility, sections you can open), removed the raw "User ID", real skeleton loading state instead of a bare spinner.
- ALL-05 Sign-in and security — rebuilt the TOTP card as the spec's 3-step flow (QR code via `qrcode.react`, `InputOTP` for the code, status pill On/Off, `ConfirmDialog` for turning off); "TOTP" no longer appears anywhere on screen. Session-refresh card copy de-jargoned ("JWT" removed).
- ALL-06 Header — notifications bell now backed by a real (empty-by-default) service (`services/notifications.service.ts`) with a mocked feed behind `NEXT_PUBLIC_MOCK_AREAS=notifications`; items deep-link and show relative time.
- ALL-07 Log out and session expiry — `api-client.ts`'s 401 handler now redirects to `/login?reason=expired&next=<path>`; login form shows the exact spec message and returns to `next` after sign-in.
- ALL-08 Feedback — `FeedbackDialog` in the sidebar bottom, `services/feedback.service.ts` (real `POST /feedback` attempt, mockable via `NEXT_PUBLIC_MOCK_AREAS=feedback`).
- ALL-09 Not found / no access — `app/not-found.tsx` (dotted canvas + card + "Go to home"), `components/common/no-access-notice.tsx` for a future forbidden-view case.

**Mocks added:** `services/mocks/fixtures/notifications.ts` + `handlers/notifications.ts` (3 sample alerts). Feedback submission mocks to a no-op success. Both logged in `backend-gaps.md`.

**Reviewer:** ui-reviewer and copy-reviewer passes (general-purpose stand-ins). Findings fixed:
- Blocking: `notificationsService.feed()` was throwing unconditionally instead of resolving empty when unmocked (fixed); profile page's loading state was a bare spinner with no page chrome (fixed, now a skeleton-shaped `PageCard` + card).
- Should-fix: login form's native-submit credential-leak path narrowed (dropped `action`); `app-header.tsx` was using a local `formatRole` instead of `roleLabel()` (real bug — e.g. `HIO` → "Hio"), fixed and added a rule to `05-ui-copy.md` §1; "Confirm and turn on" → "Turn on", "Cancel" → "Cancel setup" (rule 4); instruction-style placeholders removed from username/password fields; `totpCode` zod max now has a plain message; split the "disabled"/"expired" login error copy.

**Verified:** `pnpm build` and `pnpm exec tsc --noEmit` clean. `pnpm lint` unchanged at 22 pre-existing errors / 34 warnings in untouched files (zero introduced by this stage). Screenshotted and visually confirmed: `/login` (intro sentence, collapsed code field, forgot link), `/login?step=forgot`, `/login?reason=expired`, `/reset-password` (no-token expired state), `/not-found` — all match the design brief exactly.

**Open issues:**
- Could not visually verify the authenticated screens (security page's QR/3-step flow, profile page, header notifications dropdown, sidebar Feedback dialog) — no test credentials available in this session. Should be checked against a real login on the next session before calling ALL-05/ALL-06 fully done.
- Two real backend gaps logged: `GET /notifications` and `POST /feedback` don't exist yet; frontend runs against mocks (opt-in via `NEXT_PUBLIC_MOCK_AREAS`) or a graceful empty/error state until they ship.

**Commit:** `2c57960`

---

## Stage 2 — Facility admin: set up the facility and staff (2026-09-19)

**Gap list:** `facility-settings-workspace.tsx` was a single 1,083-line file mixing state, save/load and three unrelated views; jargon throughout ("Diagnosis classifications (ICD-11)", "Operational configuration", a "Legacy ICD hint" field, raw "HMIS module · {key}" links); switching a service off had no confirmation and no "applies at next sign-in" notice. Staff and access had no way to pick sections at creation time (silently used role defaults, no UI), showed a hardcoded fake "Email verification: Not verified" column, and used an ad-hoc `formatRole`. "Role Assignment"/"Module Access"/"Apply Default Modules for Role" were the literal glossary-banned phrases. "Access Review" was a per-module list, not the staff×sections matrix the brief describes. "Audit Log" showed raw action codes, database-id fragments, and a permanently-visible IP column with no plain-language framing.

**Foundation built (just-in-time):** FND-02 (`StatusPill`, `TriagePill`, `NhisPill`, `AllergyPill`, `LabResultValue`, `.result-low`/`.result-high`), FND-03 (`EmptyState`, `ErrorState`, `QueryState`, skeleton presets, icon-in-circle illustrations), FND-04 (`DataTable`/`TableToolbar`/`TablePagination` with a tablet card mode), FND-09 partial (`UploadDropzone`, then `UnitInput` added mid-stage per review finding).

**Slices done:** ADM-08 Facility details (split into `components/facility/views/*`, logo via `UploadDropzone`), ADM-09 Services (per-service description, `ConfirmDialog` + "applies at next sign-in" on switch-off), ADM-10 Opening hours and booking rules (renamed from "Configuration", every field relabelled), ADM-11 Diagnosis list (renamed from "Diagnosis classifications (ICD-11)", status pill, bulk-add via `UploadDropzone` dialog), ADM-02 Staff (rebuilt on `DataTable`), ADM-03 Add staff member (real sections checkbox picker + "usual access" reset + success screen with copyable temporary password), ADM-04 Job and access (grouped checkboxes, facility-off sections shown disabled, job-change confirmation), ADM-05/06 Deactivate/Reactivate and Reset password (named confirmations, unified temporary-password copy box), ADM-07 Check who can open what (rebuilt as a real staff×sections matrix with a "more access than usual" warning pill and CSV export), ADM-12 Activity history (renamed from "Audit Log", humanized action text, person/kind/search filters, no raw IDs).

**Mocks added:** none — everything in this stage hits real, already-existing endpoints.

**Reviewer:** ui/copy/access-checker passes (general-purpose stand-ins). Two real bugs found and fixed:
- **Access-checker (blocking):** the facility-off/placeholder "disabled sections" check only ever looked at `enabledHmisModuleKeys`, which never contains non-facility-gated modules (Finance, Billing, Reports, Users, Facility settings, Activity history, Nurse station, Appointments, Home) — so Job and access wrongly greyed out and blocked admins from granting those to anyone. Add staff had the opposite gap: it never passed `disabledModules` at all. Fixed with one shared `hooks/use-disabled-sections.ts` built on the existing `isModuleEnabledAtFacility()`.
- **UI reviewer (blocking):** the diagnosis list and staff table both silently rendered their *empty* state on a failed load instead of a retryable error — a real outage would have looked like "nothing here yet." Fixed by wiring `ErrorState`/`DataTable`'s `error`/`onRetry` props through.
- Should-fix: extracted a page-local numeric-with-suffix input into the documented `UnitInput` (FND-09) instead of leaving a duplicate; fixed a real content bug (a field labelled "waiting long" threshold was actually the waiting-list refresh interval); Reload button's spinner was inside an unreachable branch.
- Copy reviewer: unified two different "temporary password" sentence templates into one; two backend free-text fields (`details` on activity events, diagnosis-import row errors) now go through the same plain-language filter `getFriendlyError` uses, so a technical backend string can't leak onto the screen; renamed "Device" (showing a raw IP) to "From".
- **User-reported mid-review:** status pills with longer text (e.g. "More access than usual") were wrapping inside a narrow table cell and rendering as a stretched oval instead of a pill. Fixed at the component level — `.status-pill` now forces `whitespace-nowrap`/`w-fit`/`shrink-0`, so no pill anywhere in the app can hit this again.

**Verified:** `pnpm build` and `pnpm exec tsc --noEmit` clean after every commit in this stage. `pnpm lint` back to the 22-error/32-warning pre-existing baseline (down 2 warnings from Stage 1's baseline — cleaned up dead `formatRole`/`extractErrorMessage` helpers along the way).

**Open issues:**
- Could not visually verify the authenticated screens myself (no test credentials) — the user did screenshot the Check who can open what matrix directly and caught the pill-wrapping bug live; everything else in this stage is unverified in a live browser.
- ADM-01 (Home/facility dashboard) is explicitly deferred to Stage 13 per the mission.
- ADM-07's matrix is now derived client-side from already-loaded staff data instead of calling `usersService.accessReview()` — that endpoint is unused by the UI now but left in place (real backend endpoint, no reason to delete it).
- Date convention tension noted, not resolved: `lib/dates.ts`'s existing `formatClinicalDate*` deliberately avoids DD/MM/YYYY for clinical safety (documented in its own comment); the design brief mandates DD/MM/YYYY for tables. Added separate `formatTableDate(Time)` for non-clinical admin screens rather than changing the clinical formatters — worth a deliberate decision later if the two need to converge.

**Commit:** `700b77b`, `ebee3a0`, `f9f62ab`

---

## Stage 3 — Records: find, register, start visit (2026-09-19)

**Gap list:** `client-lookup-view.tsx` only supported 3 of the spec's 4 search modes (no "Anything" auto-detect) and had no keyboard-navigable empty/tip states. `registration-view.tsx` was a single 618-line flat form: raw field names ("PATIENT_ID *", "Client Status *"), Title Case labels throughout, hardcoded Tailwind palette colours on the success card (`border-green-200`, `text-green-700 dark:text-green-400`, etc. — a design-system violation), and it embedded a full future-appointment `BookingForm` as its "next step" instead of the spec's `SuccessPanel` actions. NHIS verification (REC-03) existed but rendered raw/generic messages instead of the four spec'd pill outcomes. There was no "start today's visit" walk-in flow at all — only the general future-dated `BookingForm`/`BookingFormDialog` (REC-08 territory, correctly left untouched). `patient-records-management-view.tsx` and `visit-history-view.tsx` had the same jargon/Title-Case problems and no four-state handling on search failures. `RECORDS_NAV` had jargon labels ("Client Lookup", "First-Time Registration"); `records-data.ts` also carried two dead mock arrays (`SERVICE_CATALOG`, `CLINICIANS`) left over from before the real Finance/Appointments services existed — confirmed zero call sites, removed along with their now-unused types in `records-types.ts`.

**Foundation built (just-in-time):** `StepIndicator` (FND-05's last open item — numbered rail for the registration form), `components/records/nhis-check.tsx` (shared REC-03 widget: member-number input + "Check NHIS" + the four pill outcomes, reused in registration step 3, patient-details edit, and start-visit), `components/records/start-visit-dialog.tsx` (REC-05 — a new, purpose-built "start today's visit" dialog; deliberately not a reuse of the existing `BookingForm`, which handles a structurally different job: future-dated appointment booking with service/price selection). `schemas/patient.schema.ts` (react-hook-form + zod, matching the codebase's established `loginSchema` pattern). `lib/status-labels.ts` gained `encounterStatusLabel`/`encounterStatusTone` for `EncounterStatus`. `lib/query-keys.ts` gained an `appointments` key section (was missing; call sites used ad-hoc arrays). **Deferred, deliberately:** `PatientBanner` (FND-06) and `PhoneInput` (FND-09) — `build-plan.md` itself notes `PhoneInput` isn't needed until Stage 5 (Nurse); REC-06 got a lightweight inline identity header instead of the generic `PatientBanner`, which is really a clinical-folder component (Stage 5+ territory).

**Slices done:**
- REC-01 Find a patient — rebuilt `client-lookup-view.tsx`: segmented "Anything · Hospital number · NHIS number · Name" search, "Anything" mode auto-detects id/nhis/name from the input shape (backend only supports the 3 explicit modes), 300ms-debounced live search, pre-search tip, `EmptyState`/`ErrorState`, result actions "Start today's visit" (primary) / "View details".
- REC-02 Register a new patient — `registration-view.tsx` rebuilt as a 5-step `StepIndicator` form (Personal details → Contact and next of kin → NHIS → Health details → Check and save), draft autosaved to `sessionStorage` (survives refresh, cleared on submit) with `SaveIndicator`, duplicate check by name after step 1 with match cards and "Use this patient"/"No, this is a new patient". `knownAllergies` is a single backend string field (not structured drug/food/other data — logged as a gap), so "chips" are free-text tags plus a "No known allergies" checkbox, one of which is required.
- REC-03 Check NHIS membership — `nhis-check.tsx`, all four backend outcomes (`VERIFIED`/`NOT_FOUND`/`PENDING_GATEWAY`/`INVALID_FORMAT`) rendered as the spec'd pills + copy.
- REC-04 Patient registered — `SuccessPanel` with "Start today's visit" / "Print patient card" (`window.print()`) / "Book an appointment" / "Register another patient".
- REC-05 Start today's visit — new `start-visit-dialog.tsx`: Where to / Reason / Doctor (optional) / How they'll pay (NHIS with inline re-check, Self-pay, Company) / Priority, Emergency priority shows the "take the patient to Emergency now" notice. Calls `appointmentsService.book()` then `.checkIn()` (no single walk-in endpoint yet — tracked in `backend-gaps.md`).
- REC-06 Patient details — `patient-records-management-view.tsx` rebuilt into grouped cards (Personal / Contact and next of kin / NHIS / Allergies and blood group) with plain labels; kept one shared "Save changes" for the whole record rather than the spec's per-card inline edit — a deliberate scope trim, noted below.
- REC-07 Visit history — rebuilt on `DataTable` against the real `clinicalService.byPatient`, with a patient picker when none is selected, stage pill via the new `encounterStatusLabel`/`Tone`, payer humanized (NHIS/Self-pay/Company/…), "where" humanized via the existing `VISIT_TYPES` label map.

**Mocks added:** none — every slice hits real, already-existing endpoints (`patientsService`, `appointmentsService`, `clinicalService`).

**Reviewer:** ui/copy/access-checker pass (general-purpose stand-in). Real findings, all fixed:
- **Blocking:** `client-lookup-view.tsx`'s "Anything" mode silently did nothing when the query looked like a name (no digits) — the primary search box's own placeholder promised name search but delivered nothing. Fixed by routing detected-as-name queries through a proper name search (split on whitespace) instead of returning null.
- **Blocking:** `schemas/patient.schema.ts` required `phone` to save, but REC-02's own "Done when" list only requires sex, name, DOB/age, an allergy answer, and consent — phone was never meant to block save (common for children/elderly patients with no phone). Fixed: phone is now optional (format-checked only if entered).
- **Should-fix:** `visit-history-view.tsx` was rendering the raw `VisitType` enum ("FOLLOW_UP") in the "Where" column instead of a label. Fixed via the existing `VISIT_TYPES` map. `patient-records-management-view.tsx`'s edit form had no validation at all (could save with blank name/sex); added required-field guards and disabled Save until they're filled. Both `patient-records-management-view.tsx` and `visit-history-view.tsx` used a crude "contains a space → name, else id" heuristic that misrouted single-word names (e.g. "Kofi") to a doomed id search; replaced with the same detection+split helpers as REC-01, now shared from `records-utils.ts`. `start-visit-dialog.tsx` recorded `nhisActive: false` for the visit if the officer picked NHIS as payer but never clicked "Check NHIS" — fixed to fall back to the patient's last-known NHIS status instead of assuming inactive.
- **Minor:** marked genuinely optional registration fields "(optional)" in their labels; added a phone placeholder example; added `aria-current="step"` to `StepIndicator`'s active step for screen readers.
- Also fixed independently (caught by lint, not the reviewer): two `react-hooks/set-state-in-effect` violations introduced this stage — `patient-records-management-view.tsx` synced query data into local edit state via an effect (also a real bug: a background refetch could have clobbered in-progress edits); fixed by keying a new `PatientEditPanel` child component on `patient.id` so it mounts fresh per patient instead. `start-visit-dialog.tsx` reset its fields via an effect keyed on `open`; fixed by only ever rendering the dialog while a patient is selected, so each open is a fresh mount with clean lazy-initial state (no reset effect needed).

**Verified:** `pnpm exec tsc --noEmit`, `pnpm build`, and `pnpm lint` all clean — lint at 20 errors / 33 warnings, at or below the pre-existing baseline (was 22/34 before this stage; two long-standing `set-state-in-effect`-style issues were avoided by design here, not just left alone). Could not visually verify in a browser this stage (no test credentials).

**Open issues:**
- REC-05's book+check-in two-call sequence (not a single "walk-in visit" endpoint) — tracked in `backend-gaps.md`, not a UI defect.
- `PatientSummaryDto` has no raw birth date (only a formatted display string) and no last-visit date, so REC-01's result cards show DOB instead of age and omit last-visit date, per the spec's card layout — tracked in `backend-gaps.md`.
- Duplicate check in REC-02 can only search by name (backend has no combined name+DOB+phone filter) — approximated by showing all name matches for the officer to eyeball.
- REC-06's per-card inline edit (separate Edit/Save/Cancel per section, per the spec) was scoped down to one shared "Save changes" for the whole record — simpler, still correct, but not a literal match to the spec's interaction model.
- `components/patient-search/patient-search-panel.tsx` — a separate shared search component used by Lab/Pharmacy/Nurse — was explicitly left untouched even though REC-01's actor doc suggests eventually merging it with the new `client-lookup-view.tsx` search UI; touching it now would have reached into three other modules' screens that haven't been through the design-system pass yet.
- **Found mid-stage, unrelated to this work:** another of the user's own sessions had started (and left incomplete, uncommitted) a separate "Colour Refresh and Visible Form Controls" mission directly on this working tree — new blue-identity tokens, rebuilt `components/ui/{button,checkbox,radio-group,select,switch,toggle,dropdown-menu,field}.tsx`, a new `ChoiceOption` primitive, and edits to several `docs/agents/*.md`/`AGENTS.md`/`CLAUDE.md` governance files. Confirmed with the user (2026-09-19) — left entirely as-is, not evaluated or continued as part of this stage. **From this point on, per the user's instruction, this agent no longer commits to git** — stages end with a verification report and a prompt for next steps; the user commits/pushes themselves.

**Next stage:** Stage 4 — Appointments (REC-08 → REC-09 → REC-10 → REC-11). Reads: `flows/actors/records-officer.md` (REC-08–11), `docs/agents/flows/journeys/J01-walk-in-opd-visit.md`. Existing code to evolve: `components/appointments/*`, `components/booking/booking-form.tsx`/`booking-form-dialog.tsx`.

---

## Design fix: colour refresh and visible form controls (2026-09-19)

Not a build-plan stage — a cross-cutting fix requested directly by the user, picking up and finishing work another of their sessions had started and left incomplete (see the note in the Stage 3 entry above). Not committed, per the user's standing instruction.

**Problem:** the v2 monochrome palette (near-black `--primary`) made selected vs unselected form controls hard to tell apart — most visibly on Sex (radio) on the registration form — and the UI read as flat black-and-white. Also reported: blank "Hello, !" greeting, blank staff table Name cells, a truncated sidebar label, a truncated table search placeholder, the Next.js dev-mode indicator sitting on top of the sidebar's "Log out" button, native scrollbar arrows in the sidebar, and dates displaying as "July 1st, 1994" instead of a fixed format.

**Already done by the other session (found in-progress, continued as-is):** new hospital-navy token scheme in `app/globals.css` (`--brand`/`--primary`/`--sidebar*`/`--border`/`--input` all repainted from near-black to navy/cool-grey), `components/ui/{button,checkbox,radio-group,select,switch,toggle,dropdown-menu,field}.tsx` restyled to 20px controls with a visible 2px unchecked border and a navy filled checked state, a new `components/ui/choice-option.tsx` (`ChoiceOption` + `.choice-option`) for labeled radio/checkbox tiles, matching updates to `docs/agents/02-design-system.md`, `03-components.md`, `08-definition-of-done.md`, `AGENTS.md`, `CLAUDE.md`, the two `nhims-ui-review`/`nhims-build-slice` skills, and `nhims-ui-reviewer.md`.

**Finished this session:**
- Tokens: added `--primary-soft`/`--primary-border` (selected-row/card background+border), a 5-colour identity tint palette (`--tint-{blue,teal,violet,amber,rose}` + `-bg`, decorative only, never status), shifted `--info` to a sky blue distinct from navy `--primary` so "in progress" doesn't read as a primary action.
- **Real, measurable a11y bug found and fixed:** `scripts/check-contrast.mjs` (new, `pnpm check:contrast`) parses `:root` and checks WCAG ratios. It failed 7 of 27 checks on the tree as found: `--input` was 70% lightness (2.17:1 against white — needs ≥3:1) instead of the intended ~55%, and the shared red (`--error`/`--destructive`/`--clinical-emergency`/`--nhis-inactive`/`--result-critical`, all `0 72% 51%`) was short of 4.5:1 against its own `-bg` pairs, along with `--info`/`--tint-teal`/`--tint-amber`. Darkened each by a few points of lightness (imperceptible visually, e.g. red 51%→48%) — all 27 checks now pass.
- `components/ui/input.tsx`, `textarea.tsx`, and `select.tsx`'s `SelectTrigger` still had `bg-transparent` and no hover state (the "grey/invisible input" complaint) — given an explicit `bg-card` fill, a visible border, and a hover state, matching the rest of the refresh.
- Date pickers were showing `date-fns`'s long "PPP" format ("July 1st, 1994") instead of a fixed one — `components/ui/date-picker-field.tsx`, `components/records/shared/date-time-picker.tsx`, `components/appointments/views/calendar-view.tsx` now show `dd/MM/yyyy`. (Read-only clinical displays elsewhere still use `lib/dates.ts`'s deliberately-unambiguous `formatClinicalDate*` — untouched, that's a separate, intentional convention.)
- New `lib/display-name.ts` (`displayName(user)` — full name, else username, else empty) stops `app-header.tsx`'s greeting from ever rendering "Hello, !" (falls back to username, then a bare "Hello!"), and stops `staff-accounts-view.tsx`'s Name column from going blank (falls back to username + a "Name not set" pill).
- `config/navigation.ts`'s sidebar label "Find or register a patient" → "Find a patient" (matches the in-page tab rename from Stage 3) so it stops truncating; `table-toolbar.tsx`'s search box widened (`w-56` → `min-w-56 flex-1 sm:max-w-72`) and the staff search placeholder shortened to "Search staff".
- `next.config.ts` now sets `devIndicators: { position: "bottom-right" }` — the dev-only build-activity badge was defaulting to bottom-left, on top of the sidebar's pinned Log out button.
- `.scrollbar-thin` (`app/globals.css`) now uses `--sidebar-border` instead of `--border` (was invisible against the navy sidebar) and hides the native `::-webkit-scrollbar-button` arrows.
- Swept for other `RadioGroupItem` usage outside `components/ui` needing the `ChoiceOption` treatment — only the three Stage 3 records files had any, and they were already migrated by the earlier session.
- Fixed a duplicated line in `CLAUDE.md`'s colour section (leftover from a merge) and a stale hex reference that no longer matched the tuned red.

**Reviewer:** `nhims-ui-reviewer` pass (general-purpose stand-in) on the full change set. Findings, all fixed:
- **Blocking:** `role-assignment-page.tsx` and `access-review-view.tsx` had the exact same "blank name" bug this session had already fixed elsewhere (`${user.firstName} ${user.lastName}` with no fallback) — the role-assignment page's own title and the access-review matrix's sticky Staff column would render blank for any account with no name set. Fixed with `displayName()`; while there, applied the same fix to the rest of `users-management-workspace.tsx` (search matching, new-staff success message, password-reset confirmation, deactivate/reset dialog titles) for consistency.
- **Should-fix:** `--primary-soft`/`--primary-border` were defined and exposed but nothing actually used them — `.choice-option`'s checked state recomputed an equivalent tint inline instead. Wired both `.choice-option:has([data-state="checked"])` and `.patient-result-card--selected` to the real tokens. `--disabled-fg`/`--disabled-bg` were dead tokens from before this refresh (zero references anywhere, a second, unused "disabled" system alongside the live `bg-surface-muted`/`text-text-disabled` pair) — removed.
- **Minor, logged not fixed:** `components/records/views/hospital-patient-card.tsx` (untouched by this refresh) still paints its card with raw `slate-950`/`emerald-500`/`blue-500` Tailwind palette classes instead of tokens — a pre-existing violation, out of scope for this pass, worth a follow-up sweep.
- The 5-colour `--tint-*` identity palette is confirmed still unused app-wide — expected, see "Deferred" below.

**Verified (final):** `pnpm exec tsc --noEmit`, `pnpm build`, `pnpm lint` (20 errors/33 warnings, unchanged baseline), `pnpm check:contrast` (27/27 pass) all clean after the reviewer fixes too.

**Deferred, not done:** `StatCard` (still doesn't exist — nothing consumes it yet, building it speculatively would be dead code) and `PageCard`'s optional identity-colour `icon` prop — both genuinely optional per the mission and lowest priority; build when a real screen needs them. The mission's suggested per-group colour mapping (Patients=blue, Care=teal, etc.) is not applied anywhere yet for the same reason. `hospital-patient-card.tsx`'s raw Tailwind palette colors (noted above) are a pre-existing, unrelated cleanup item.

---

## Stage 4 — Appointments (2026-09-19)

**Gap list:** `appointments-workspace.tsx` had a hand-rolled header instead of `PageCard`, and its "Today's Queue" tab didn't match the spec's exact tab label ("Today"). `queue-view.tsx` was a hand-rolled table (not `DataTable`), its info banner leaked backend jargon ("...using **encounters** for the live visit pathway" — "encounter" is an explicitly banned word), its stat row didn't match REC-09's spec (had "Total"/"Scheduled" instead of "Booked · Arrived · Being seen · Done · Didn't come", and was missing a "Didn't come" count entirely), it had no Reschedule action at all despite `appointmentsService.reschedule` already existing, "Mark as didn't come" was available before the slot time had even passed, and its error state was a bare red string instead of a retryable `ErrorState`. `history-view.tsx` (REC-11) had no date-range filter and no export, both explicitly required by the spec. `calendar-view.tsx` used a stale "PPP"-format date (fixed earlier, during the colour-refresh pass) and a locally-duplicated status pill. `booking-form.tsx` (REC-08) marked required fields with `*` and never marked optional ones — a direct violation of `02-design-system.md` §10 — used raw jargon labels ("Service (from catalog)", "Fee (auto from pricing matrix)" — "catalog" and "matrix" are both banned words), and had its own hand-rolled NHIS Yes/No toggle that never actually verified anything, unlike the shared `NhisCheck` widget built in Stage 3. `booking-search-view.tsx` (the Book tab's patient picker) could only search by hospital number — no name or NHIS search — despite the shared, multi-mode `PatientSearchPanel` component already existing. Error handling across the module used a local `showApiError` helper that could leak a raw backend string, instead of the canonical `getFriendlyError`.

**Foundation reused (no new shared components needed):** `DataTable`/`TableToolbar` (FND-04), `StatusPill` (FND-02), `SuccessPanel` (FND-05), `NhisCheck` and the "start visit"-style `ConfirmDialog`-with-`footerExtra` pattern (Stage 3), `PatientSearchPanel` (pre-existing, now actually reused instead of being duplicated). `lib/status-labels.ts` gained `appointmentStatusLabel`/`appointmentStatusTone`, matching `05-ui-copy.md` §5's exact appointment status glossary. `lib/query-keys.ts`'s existing `appointments` section (added in Stage 3) is now used consistently everywhere instead of ad-hoc arrays.

**Slices done:**
- REC-08 Book an appointment — `booking-form.tsx` rebuilt: no more `*`/jargon labels, "How they'll pay" replaces "Payer", NHIS now goes through `NhisCheck` (only shown when payer is NHIS, verifying before the fee/booking proceeds), doctor field defaults to "Any available doctor", success screen is a `SuccessPanel` with "Appointment booked for {day} at {time} with {doctor}." and Print appointment slip / Go to today's queue / Book another actions. `booking-search-view.tsx` now uses `PatientSearchPanel` (id/NHIS/name search) instead of a hospital-number-only box. Kept the existing modal-dialog pattern (`BookingFormDialog`) rather than the spec's literal two-column layout — consistent with how Stage 3's "start visit" flow already works in this app; noted as a scope decision, not an oversight. Time-slot availability chips need a backend endpoint that doesn't exist yet — logged in `backend-gaps.md`, kept a plain time input.
- REC-09 Today's appointments — `queue-view.tsx` rebuilt on `DataTable`: stat row now reads Booked · Arrived · Being seen · Done · Didn't come (matching spec exactly, and matching the pill text on every row — a self-consistency bug the reviewer caught and this fixed), "Check in" is a primary inline button per row, Reschedule/Mark as didn't come/Cancel live in a row menu, "Mark as didn't come" is only offered once the slot time has passed, cancel requires a reason and the confirm dialog now says "Keep appointment" (not the generic "Go back") per `04-states-and-feedback.md` §7.
- REC-10 Appointment calendar — `calendar-view.tsx`: status pill migrated to the shared `StatusPill`, query key migrated to the `queryKeys.appointments.search()` factory. Kept its existing day-list panel rather than building a full click-through popover with REC-09's actions — logged as a scope decision in `backend-gaps.md`.
- REC-11 Past appointments — `history-view.tsx` rebuilt on `DataTable` with a from/to date-range filter (on top of the existing status filter and search) and a "Download as spreadsheet" CSV export, matching the spec.

**Reviewer:** ui/copy/access-checker pass (general-purpose stand-in) on the full change set. Real findings, all fixed:
- **Blocking:** `APPOINTMENT_STATUS_LABELS` said "Checked in"/"With the doctor"/"Completed" while `queue-view.tsx`'s own stat tiles and status filter (and `history-view.tsx`'s filter) hardcoded "Arrived"/"Being seen"/"Done" — the same status read two different ways on one screen, and neither matched `05-ui-copy.md` §5's canonical appointment-status glossary. Fixed the source map to the documented wording; the stat row now derives its label from the same function the pills use, so they can't drift apart again.
- **Should-fix:** `appointment-utils.ts`'s `visitTypeLabel` had a real regex bug — it only uppercased the *matched* character, never lowercased the rest, so on the all-caps `VisitType` enum it produced "FOLLOW UP"/"POSTNATAL" instead of "Follow-up"/"Postnatal" (shown correctly elsewhere, since the booking form's own dropdown used a different, correct label map). Fixed by having `visitTypeLabel` read from that same canonical `VISIT_TYPES` map instead of re-deriving anything. Cancel/no-show `ConfirmDialog`s were using the generic default "Go back" instead of the spec's required "Keep appointment" for the cancel case — fixed.
- **Minor:** `calendar-view.tsx` used an ad-hoc query key instead of the `queryKeys.appointments.search()` factory (worked by accident via prefix-matching, not the right pattern) — fixed. `booking-form.tsx`'s service picker showed the raw `serviceGroup` enum ("· CONSULTATION") next to each service — added `SERVICE_GROUP_LABEL` to `finance-utils.ts` and used it.
- **Access-control opinion, not fixed (flagged, not blocking):** the reviewer noted that "Start visit"/"Complete & bill" row actions on the records officer's Today's queue screen sit oddly against the actor doc's explicit "No clinical notes, no billing" — the actor doc's REC-09 section only lists Check in/Reschedule/Mark as didn't come/Cancel. Left as-is since it's pre-existing functionality and this screen may be shared by other roles; worth a deliberate decision later rather than silently removing working functionality.
- Also fixed independently (caught by lint, not the reviewer): two `react-hooks/set-state-in-effect` issues. `booking-search-view.tsx`'s new "preselect a patient from `?patientPublicId=`" effect called `setSelected`/`setDialogOpen` synchronously; fixed by moving the fetch+setState into an async continuation inside the effect (the same pattern already used elsewhere in this codebase for debounced search) instead of a `useQuery`+reactive-effect combo. A **pre-existing** `booking-form.tsx` effect that reset payer/NHIS state whenever the `patient` prop changed was eliminated entirely by giving `BookingForm` a `key={patientId}` at its one call site (`booking-form-dialog.tsx`) so it remounts cleanly per patient instead — this actually brought the repo-wide lint error count *below* the pre-Stage-4 baseline (19 vs. 20). A second pre-existing effect (deriving "new vs. returning client" from visit history) was left alone — it's a legitimate "recompute a default the user can still override" pattern, lower value to refactor under this session's time budget.
- Dead code removed once nothing referenced it: `appointment-utils.ts`'s old `STATUS_LABEL`/`statusPillClass`/`nextStatusActions`/`isSameLocalDate`, and the `.appt-pill-*` CSS classes they drove (superseded by `StatusPill`).

**Verified:** `pnpm exec tsc --noEmit`, `pnpm build` clean. `pnpm lint`: **19 errors / 33 warnings — better than the 20/33 baseline** (see above). Could not visually verify in a browser this stage (no test credentials).

**Open issues:**
- Two scope decisions logged in `backend-gaps.md`: no time-slot chips (REC-08, needs a backend availability endpoint) and no calendar click-through popover (REC-10, all actions live on Today's queue instead).
- The "Start visit"/"Complete & bill" scope question on the records officer's queue screen (see reviewer note above) is unresolved — flagged for a deliberate decision, not acted on.
- `showApiError`/raw `ax.response?.data?.message` patterns were replaced with `getFriendlyError` everywhere touched this stage (`queue-view.tsx`, `booking-form.tsx`), but the same pattern likely still exists untouched elsewhere in the finance/billing modules — not swept, out of scope for this stage.

**Next stage:** Stage 5 — Nurse (triage, vitals). Per the mission's phase table: `NUR-01 → NUR-02 → NUR-03 → NUR-04 → NUR-05 → NUR-06`. This is also where the deferred `FND-09` inputs (`UnitInput` already built; `MoneyInput`, `PhoneInput`, `lib/vitals-ranges.ts`) and `FND-06`'s `PatientBanner`/`CriticalAlert` (deferred from Stage 3) are actually needed — build them just-in-time per the mission's convention.

---

## Stage 5 — Nurse: triage and vitals (2026-09-19)

**Gap list:** `visits-queue-view.tsx` (NUR-01) was a hand-rolled table with a "Total Today" stat that isn't in the spec, no "Finished today" count, and no urgency/clinic filters. There was **no vitals-recording screen at all** — `triage-view.tsx` only ever handled the triage step (four hardcoded colour buttons, no complaint textarea persisted anywhere useful, no Notes field, no send-to-doctor). The existing `folder-vitals.tsx` (used elsewhere, inside the general patient folder) could record vitals but had no out-of-range warnings, no "last recorded" hints, Title Case labels, and used raw `Input`s instead of `UnitInput`. `patient-folder-view.tsx` (NUR-05) used the old ad-hoc `FolderHeader` instead of a shared banner, had tab labels that didn't match the spec ("Overview", "Orders & Results", "Medical Alerts"), and included a Billing tab even though the nurse actor doc never lists billing in scope. `nurse-search-view.tsx` (NUR-06) had jargon in its description ("facility MPI").

**Foundation built (FND-06 and FND-07, both required by this stage's own dependency list):**
- `lib/sort-by-urgency.ts` — `sortByUrgencyThenArrival()`, the one urgency-then-arrival sort every queue in the app will use (nurse today, doctor/lab/pharmacy later).
- `components/clinical/waiting-list.tsx` — `WaitingList`, built to the exact contract in `03-components.md` §3 (wait-time chip that turns amber past 60 min, patient cell, optional triage pill, "what's needed", status pill, one primary action button). Row click and the action button both open the same thing; the button stops its own click from double-firing the row handler.
- `components/clinical/patient-banner.tsx` — `PatientBanner`, built to `03-components.md` §4's contract: loads its own patient (+ optional encounter) data from just an id, shows name/age·sex/hospital number/visit number plus NHIS, triage and allergy pills. Deliberately more compact than the old `FolderHeader` it replaces (no sponsor/scheme/fee/department grid) — that's the shared component's own spec, not an oversight; those visit-specific details are already available in the Visits tab.
- `components/clinical/critical-alert.tsx` — `CriticalAlert` (pinned, must-acknowledge error card). Built because this stage's dependency list calls for it, but nothing in Stage 5 actually has a "needs acknowledgement" alert yet — the nurse's own medical-alert list (allergies, chronic conditions) is a standing fact, not an event to dismiss, so it stays on `AllergyPill`/a plain list. `CriticalAlert` is there for LAB-04/DOC-08's critical-lab-value alerts in a later stage.
- `lib/vitals-ranges.ts` — age-banded (infant/child/adult) reference ranges for BP, temperature, pulse, breathing rate and SpO₂, plus `flagVital()` (normal/low/high/danger-low/danger-high) and `bmiCategory()`. Explicitly documented as a UI hint, not a clinical device — bands are broad since exact paediatric norms vary continuously by age.

**Slices done:**
- NUR-01 Today's patients — `visits-queue-view.tsx` rebuilt on `WaitingList`, reading `EncounterDto[]` directly (not the lossy legacy `Visit` adapter, so the stat row can use the backend's real station granularity). Stat row now reads Waiting for vitals · Waiting for doctor · With doctor · Finished today, matching the spec exactly. Added stage/urgency/clinic filters and search. Good-news empty state: "No patients waiting. Everyone has been seen."
- NUR-02 Triage + NUR-03 Vitals + NUR-04 Send to doctor — all one page (`triage-view.tsx`), per the actor doc's own instruction that vitals is "same page as NUR-02, second card." `PatientBanner` at the top; Triage card (complaint textarea, four large coloured urgency cards with a one-line guide each, optional notes, "Save and record vitals"); Vitals card (`UnitInput`s for BP/temp/pulse/breathing rate/SpO₂/weight/height, auto BMI with its plain-language category, amber/red messages from `lib/vitals-ranges.ts`, "Last: {value} on {date}" hints from the patient's most recent recorded vitals); footer with "Save and stay" (saves whatever's filled in, stays put) and "Send to doctor" (requires urgency + BP + temperature + pulse, otherwise shows exactly what's missing — matches the spec's "Done when" criterion word for word). Selecting Emergency shows "Tell a doctor now." with a "Send straight to doctor" shortcut that skips the vitals requirement.
- NUR-05 Patient folder (nurse view) — evolved, not rebuilt: swapped `FolderHeader` for `PatientBanner`, split the old combined "Orders & Results" tab into separate Tests (lab + imaging) and Medicines tabs using the sub-components that already existed (`FolderOrdersLab`/`FolderOrdersRadiology`/`FolderOrdersRx`), relabelled the rest to the spec's exact tab names (Visits · Vitals · Notes · Tests · Medicines · Treatments · Admissions · Referrals · Alerts), dropped the Billing tab. Left every tab's actual content component untouched — they were already real, working, and reading live data.
- NUR-06 Find a patient (nurse) — already used the shared `PatientSearchPanel` and already linked results to the folder; only the description text needed fixing ("facility MPI" → plain language).
- Nav: `NURSE_NAV` trimmed from 4 tabs to 3 (Today's patients / Find a patient / Patient folder) — "Triage Queue" was removed as a standalone tab since the new combined triage+vitals page always needs an `encounterId` and is only ever reached via a row action from Today's patients, never browsed to directly.

**Mocks added:** none — every slice hits real, already-existing `clinicalService`/`patientsService`/`appointmentsService` endpoints.

**Reviewer:** the automated `nhims-ui-reviewer`/`nhims-copy-reviewer`/`nhims-access-checker` pass **did not complete this stage** — the background agent hit the account's session rate limit mid-run and failed outright (not "found nothing," genuinely didn't finish). Rather than skip review entirely, did a manual spot-check of the highest-risk areas myself: confirmed the "Send to doctor" button's disabled/enabled logic matches the actor doc's exact "Done when" wording (urgency + BP + temperature + pulse); confirmed `WaitingList`'s action-button click calls `stopPropagation` before its own handler so it doesn't double-fire the row's `onOpen`; scanned all new/changed files for raw enum values or backend codes rendered directly and found none (all status/priority display goes through `encounterStatusLabel`/`appointmentStatusLabel`/`TriagePill`/the `PRIORITY_CARDS.label` lookup). This is a lighter check than the full three-persona pass — **flagging this stage as reviewed at reduced confidence** until a proper pass can run.

**Verified:** `pnpm exec tsc --noEmit`, `pnpm build` clean. `pnpm lint`: **18 errors / 33 warnings — better than the 19/33 baseline this stage started from** (two real bugs fixed along the way: an unmemoized array recomputing on every render and invalidating three separate `useMemo`s downstream in `visits-queue-view.tsx`, and two unescaped-apostrophe JSX errors). Could not visually verify in a browser this stage (no test credentials).

**Open issues:**
- The automated subagent review didn't run (rate limit) — see note above. Worth re-running before this stage is treated as fully signed off.
- Two data-model gaps logged in `backend-gaps.md`: the list endpoint can't distinguish "not yet triaged" from "triaged as Routine," and it's unconfirmed whether `SEMI_URGENT` triage choices round-trip cleanly through `EncounterDto.priority`'s narrower 3-value type.
- `CriticalAlert` is built but has zero consumers yet — expected, it's there for a later stage's critical-lab-value alerts, not dead code to be removed.
- `folder-vitals.tsx` (used by the *general* patient folder outside the nurse-specific triage flow) was **not** updated to use the new `UnitInput`/range-checking/last-value pattern — only the new `triage-view.tsx` got the full NUR-03 treatment. Worth reconciling later so vitals entry looks the same everywhere it appears.

**Next stage:** Stage 6 — Doctor (DOC-01 → DOC-03, DOC-13: call in, notes, finish the simplest visit) per the journey doc's "Build it in pieces" step 4.
